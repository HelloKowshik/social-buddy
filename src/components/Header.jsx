import { Typography } from '@material-ui/core';
import React from 'react';

const Header = () => {
    return (
        <div>
            <Typography variant='h2' align='center' color='primary'>Social Buddy</Typography>
            <hr />
        </div>
    );
};

export default Header;